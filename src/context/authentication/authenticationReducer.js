import {
  SIGNUP_FAIL,
  SIGNUP_SUCCESS,
  LOGIN_ERROR,
  LOGIN_SUCCESS,
  LOGOUT,
  GET_ACTIVE_USER,
} from "../../types";
export default function reducer(state, action) {
  switch (action.type) {
    default:
      return state;
  }
}
